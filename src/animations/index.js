/**
 * Reusable animation class names (defined in /src/styles/animations.css).
 * Keeping these as named exports avoids typo'd class strings scattered
 * across screens/components.
 */
export const ANIMATIONS = {
  fadeIn: "fade-in",
  pulseRing: "pulse-ring",
};
